{
  "name": "util.filelist",
  "version": "0.0.22",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Reads a file list and returns it as an array of values.",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.filelist"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.filelist/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "fs-extra": "^3.0.1",
    "util.pkgwrap": "0.0.57",
    "util.rstrip": "0.0.21"
  },
  "devDependencies": {
    "@types/fs-extra": "3.0.2",
    "@types/node": "^7.0.23",
    "@types/uuid": "^2.0.29",
    "ava": "^0.19.1",
    "coveralls": "^2.13.1",
    "expand-home-dir": "github:jmquigley/expand-home-dir",
    "nyc": "^11.0.1",
    "tslint": "^5.4.0",
    "typescript": "^2.3.4",
    "uuid": "^3.0.1"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "all": "npm install && npm run clean && npm run lint && npm install && npm run build && npm test && npm run report",
    "build": "pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "lint": "pkgwrap --lint",
    "postinstall": "pkgwrap --postinstall",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
