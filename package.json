{
  "name": "util.filelist",
  "version": "0.0.19",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Reads a file list and returns it as an array of values.",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.filelist"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.filelist/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "fs-extra": "^2.1.2",
    "util.pkgwrap": "0.0.26",
    "util.rstrip": "0.0.16"
  },
  "devDependencies": {
    "@types/fs-extra": "2.1.0",
    "@types/node": "^7.0.13",
    "@types/uuid": "^2.0.29",
    "ava": "^0.19.1",
    "coveralls": "^2.13.0",
    "expand-home-dir": "github:jmquigley/expand-home-dir",
    "nyc": "^10.2.0",
    "tslint": "^5.1.0",
    "typescript": "^2.2.2",
    "uuid": "^3.0.1"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "all": "npm install && npm run lint && npm install && npm run build && npm test && npm run report",
    "build": "./node_modules/.bin/pkgwrap --build",
    "coverage": "./node_modules/.bin/pkgwrap --coverage",
    "lint": "./node_modules/.bin/pkgwrap --lint",
    "postinstall": "./node_modules/.bin/pkgwrap --postinstall",
    "report": "./node_modules/.bin/pkgwrap --reporting",
    "test": "./node_modules/.bin/pkgwrap --testing --ava"
  }
}
